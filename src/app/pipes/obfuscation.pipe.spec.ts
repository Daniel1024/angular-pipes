import { ObfuscationPipe } from './obfuscation.pipe';

describe('ObfuscationPipe', () => {
  it('create an instance', () => {
    const pipe = new ObfuscationPipe();
    expect(pipe).toBeTruthy();
  });
});
